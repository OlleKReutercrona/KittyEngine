using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RenderLayer : MonoBehaviour
{
    public enum Layer
    {
        Back,
        Main,
        Front,
        UI
    }

    public Layer layerToRenderOn;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
